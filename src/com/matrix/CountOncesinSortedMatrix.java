package com.matrix;

import com.utils.Utils;

public class CountOncesinSortedMatrix {

    public static void count(int[][] matrix) {
    }/*{
        if (Utils.isNullOrEmpty(matrix))
            return;

        int m = matrix.length, n = matrix[0].length;
        int end = m, st = 0;
        //Index
        int lastMid = 0;
        //Index
        int finalMid = 0;

        for (int i = 0; i < m; i++) {
            int mid = (st + end) / 2;
            if (mid < end && mid > 0) {

                midValue = matrix[i][mid];
                if (finalMidValue > midValue) {
                    finalMidValue = midValue;
                    maxInt = m-1-
                }

                if (matrix[i][mid] == 0)
                    st = mid + 1;
                else
                    end = mid;
            }
        }
    }*/

    public static void main(String[] args) {
        int i = 1;
        System.out.println(i);
    }

}
